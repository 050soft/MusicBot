// File that will be created in /build by the version manager.
// UPLOAD /build to the server running the code; do NOT build it on the server.
export const version: string;
export const commit_id: string;